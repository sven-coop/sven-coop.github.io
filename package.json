{
	"name": "sc_as_docs",
	"version": "1.0.0",
	"description": "",
	"main": "build.js",
	"type": "module",
	"scripts": {
		"clear": "rm -rf dist && mkdir dist",
		"build": "npm run clear && node build.js",
		"deploy": "npm run clear && node build.js && gh-pages -d dist"
	},
	"keywords": [],
	"author": "",
	"license": "MIT",
	"dependencies": {
		"handlebars": "^4.7.7"
	},
	"devDependencies": {
		"gh-pages": "^5.0.0"
	}
}
